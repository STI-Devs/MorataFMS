min_version: 1.11.0
skip_lfs: true
source_dir: scripts/lefthook

pre-push:
  scripts:
    workspace-ci.sh:
      runner: bash
      fail_text: Local workspace checks failed. Fix the issues locally or use --no-verify if you intentionally need to bypass the hook.
